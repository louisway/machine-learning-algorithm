import numpy as np

def train_nn(data,units_num,epoch,threhold):
#assume the type of data is numpy.array
#assume the type of units_num is numpy.array
#assume units_num includes the number of cells in each layer except input layer 
	weights=[]
	n,m=data.shape
	#initial weights
	row=m
	for col in units_num:
		weight=np.random.random((row,col))
		weights.append(weight)
		row=col

	for _ in range(epoch)
		input=[]
		input.append(data[i,:])
		input,weight=forward_pass(input,weights)
	